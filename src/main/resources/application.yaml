spring:
  application:
    name: springflix

  datasource:
    url: jdbc:postgresql://localhost:5432/springflix
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    database-plataform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  flyway:
    enabled: true
